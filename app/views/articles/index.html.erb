<h1>Wikinimous</h1>

<% @articles.each do |article| %>
  <h2>
    <%= link_to article.title, article_path(article.id) %>
  </h2>
  <p>
    <%= link_to article.content, article_path(article.id) %>
  </p>
<% end %>


<%= link_to "Add a new article", new_article_path %>
